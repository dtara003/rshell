#!/bin/sh

	#empty string
cd Dir #cd
ls -a #ls -a command
date; #date command
git status #git command
ls; # with semicolon
ls #without semicolon
xyz #invalid commands 
x y z #invalid commands with spaces
mkdir #commands with missing arguments
git branch; #git command
ls -l 
l s; #valid commands with space in between 
ls s #command with invalid argument
git 
echo hello world #echo with spaces in between
echo echo #echo echo
echo "echo" #echo with quotes
touch test.cpp #touch command
vim test.cpp #vim command
cat test.cpp #cat command
rm test.cpp #remove file
mkdir Dir #make directory
rm -r Dir #remove Directory
|| echo hello
&& echo hello
