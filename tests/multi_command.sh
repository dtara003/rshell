#!/bin/sh

#go to the directory of where a.out is(rshell in this case)
cd ..

echo hello || ls && date < ./a.out
ls && xyz && echo hey && date < ./a.out
ls -a ||  sup && git branch < ./a.out
cd Dir && mkdir Dir < ./a.out
mkdir Dir && rm -r Dir || echo hey < ./a.out
