#include "Shell.h"

#include <iostream>
#include <vector>
#include <sstream>
#include <unistd.h>

//comment

using namespace std;

int main() {
    /*while(1) {

       */ Shell s;
        s.prompt();

        /*string input;
        getline(cin, input);
        
        stringstream str(input);
        string s;
        
        vector <string> words;

        while (str >> s) {
            words.push_back(s);

            if (s == "exit") {
                return 0;
            } else {
                cout << s << endl;
            }
        }

        // check
        // cout << input << endl;
    }*/

    return 0;
}
